export type { Config, DiffConfig, IgnoreConfig, MentionsConfig, PollConfig, StorageConfig } from "./schema"
export { defaultConfig, defaultIgnorePatterns } from "./defaults"
export { loadConfig } from "./loader"
