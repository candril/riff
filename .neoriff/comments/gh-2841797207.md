---
id: gh-2841797207
filename: src/Dg.GalaxusAbos.HttpApi/GraphQL/Contracts/ContractDisplayNameMutations.cs
line: 44
side: RIGHT
createdAt: 2026-02-23T16:25:46Z
status: synced
commit: ba5a5eacaa01439cadda98f6965c40f271bcc522
githubId: 2841797207
githubUrl: https://github.com/DigitecGalaxus/Dg.GalaxusAbos/pull/391#discussion_r2841797207
author: candril
---

this applies for all contract types 

<!-- context -->
```diff
@@ -0,0 +1,94 @@
+using Chabis.Application.Authentication.Jwt;
+using Dg.GalaxusAbos.Contracts;
+using Dg.GalaxusAbos.HttpApi.GraphQL.Contracts;
+using Dg.GalaxusAbos.HttpApi.ViewerMiddleware;
+using Dg.GalaxusAbos.OutgoingApi.AAX.ServiceData.Internal;
+using HotChocolate;
+using HotChocolate.Authorization;
+using HotChocolate.Types.Relay;
+
+namespace Dg.GalaxusAbos.HttpApi.GraphQL;
+
+public partial class Mutation
+{
+    [Authorize(DgPolicies.MinimumTrustLevelLow)]
+    public async Task<GalaxusAbosUpdateContractDisplayNamePayload> GalaxusAbosUpdateContractDisplayNameAsync(
+        GalaxusAbosUpdateContractDisplayNameInput input,
+        [Service] IViewerContextInfo viewerContextInfo,
+        [Service] ContractRepository contractRepository,
+        [Service] OutgoingApi.AAX.ServiceData.IContractService serviceDataService,
+        CancellationToken cancellationToken
+    )
+    {
+        var contractId = long.Parse(input.ContractId);
+        var customerId = viewerContextInfo.Customer.CustomerId;
+
+        var contract =
+            await contractRepository.GetContractAsync(contractId, cancellationToken)
+            ?? throw new UnauthorizedAccessException($"Contract {contractId} not found");
+
+        if (viewerContextInfo.Family.FamilyMembers.All(m => m.CustomerId != contract.CustomerId))
+        {
+            throw new UnauthorizedAccessException($"Access denied to contract {contractId}");
+        }
+
+        var isOwnService = contract.CustomerId == customerId;
+
+        if (!isOwnService)
+        {
+            if (contract.ServiceType == ServiceType.Internet)
+            {
+                throw new UnauthorizedAccessException(
+                    "Only the contract owner can modify internet contract display names"
+                );
+            }
```