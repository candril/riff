/**
 * Follow link (gf / gF / gx)
 *
 * - gf: the file under the cursor — inside riff when the diff has it
 * - gF: the same, in a new tmux window
 * - gx: the URL under the cursor, in a browser
 */

export { handleFollowLink, type FollowLinkContext } from "./handlers"
